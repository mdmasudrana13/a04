1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
ANS : getElementById = HTML ONLY ONE ID SELCET FOR USE [ID CAN USE ONLY ONE ]
     getElementsByClassName Mens you can select all calss [class is pluar number so calss can use many time]
      querySelector / querySelectorAll querySelector mens you can seclect anty taype id calss tag 

2. How do you create and insert a new element into the DOM?
.creacteElemt.('elemetn name')
elemet name.innerText
3. What is Event Bubbling? And how does it work?
Bubbling like a tree you can goo root -main and main to root
4. What is Event Delegation in JavaScript? Why is it useful?
Delegation is selcet child to praent 
5. What is the difference between preventDefault() and stopPropagation() methods
preventDefault() is a JavaScript method often used in event handling  stops the browsers  action that would normally happen when an event occurs
topPropagation()